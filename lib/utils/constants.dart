import 'package:flutter/material.dart';

// Este archivo define los colores y estilos de texto que se usarán en toda la aplicación,
// lo que permite mantener una apariencia consistente y facilita la actualización de la
// estética de la aplicación en el futuro. Al centralizar estos valores, se mejora la
// mantenibilidad y se reduce la duplicación de código en los widgets individuales.

const kBackgroundColor = Color(0xFF0E0E10); // muy oscuro
const kCardColor = Color(0xFF131417);
const kAccentYellow = Color(0xFFFFC400);
const kMutedText = Color(0xFFB7B7B7);

final kTitleStyle = TextStyle(color: Colors.white, fontSize: 20, fontWeight: FontWeight.bold);
final kSubtitleStyle = TextStyle(color: kMutedText, fontSize: 14);